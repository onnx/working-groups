(**
    Specification of Neg operation on tensors.
 *)

module Neg
  use int.Int
  use map.Map
  use utils.Same
  use tensor.Shape
  use tensor.Tensor

  let function neg (a : tensor 'a) : tensor 'a =
  {
    shape = a.shape ;
    value = fun i -> - a.value[i] ;
  }

end